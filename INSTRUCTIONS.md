# Instruções para executar a aplicação

## Backend

No diretório raiz do projeto, execute o seguinte comando:

```bash
./gradlew bootRun
Ou roda o Application.java
```

## Frontend

No diretório `serveme-frontend`, execute o seguinte comando:

```bash
npm run dev
```
